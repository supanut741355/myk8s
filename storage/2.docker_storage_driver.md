# Docker Storage Driver / volume driver plugin

storage driver: resposibility = help manage storage on images and containers.

volumes handle by volume driver plugin.

default volume plugin is local


can use specific volume_drive 
```
$ docker run -it \
  --name mysql
  --volume-driver rexray/ebs
  --mount src=ebs-vol, target=/var/lib/mysql
  mysql
```