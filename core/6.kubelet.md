# Kubelet

1. Contract from master node to load or unload the container on the worker node
  - register node
  - create pod
  - monitor node and pods
2. Requst to container runtime to run the target container 
  - run the container