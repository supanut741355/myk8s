
ClusterIP

is type of service for **internal commnunicate** each apps (service-to-service)

if we create ClusterIP it will create internal IP for service

We create serviec-clusterIP (internalIP) for group of app, service will act like a **loadbalancer**

eg.

frontend-app -> service(clusterIP) -> backend-app

can communicate internal with
- ip
- DNS (xxx.ns.svc.cluster.local)