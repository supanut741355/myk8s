Kube Controller Manager

- Watch status 
- Remediate situation


have multiple type of controllers:

1. Node controller -> responsible for monitoring the status of node 

2. Replication controller -> responsible for monitoring status of replicaSet and ensure the desired number of pod

3. more (deployment controller, job, cronjob controller, namespace controller)