Kube proxy 

every pod on diff node can reach together 

![](/assets/overviews/kube-proxy.png)


1. Deploy web app on 1st node, and db on the 2nd node

2. The web app can reach the db by using ip via pod network -> but not graruntte the ip of pod still the same 

3. to fix this issue we will create the **sevice** 

4. the service will forward the requrst between web app and db

5. the kube proxy will run on each node 


